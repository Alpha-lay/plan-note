# PlanNote 启动和部署指南

## 开发环境启动

### 1. 安装依赖
```bash
npm install
```

### 2. 启动开发服务器
```bash
npm run dev
```
应用将在 `http://localhost:5173` 启动

### 3. 构建生产版本
```bash
npm run build
```
构建产物将在 `dist/` 目录生成

### 4. 预览生产版本
```bash
npm run preview
```
在本地预览构建后的应用

## 部署到 Vercel

### 1. 上传到 GitHub
1. 初始化 git 仓库
```bash
git init
git add .
git commit -m "Initial commit: PlanNote application"
```

2. 创建 GitHub 仓库并推送
```bash
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPOSITORY_NAME.git
git branch -M main
git push -u origin main
```

### 2. 在 Vercel 部署
1. 访问 https://vercel.com 并登录
2. 点击 "New Project"
3. 选择 "Import Git Repository"
4. 选择您的 GitHub 仓库
5. 确认配置：
   - Framework: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`
6. 点击 "Deploy"

## 应用功能说明

### 任务管理
- 点击 "+" 创建新任务
- 设置任务时间（HH:MM 格式）
- 选择优先级：urgent (🔥) 或 chill (☕)
- 开启/关闭 WeChat 提醒
- 点击复选框标记完成

### 笔记管理
- 切换到 Notes 标签
- 点击 "+" 创建笔记
- 可添加标签分类
- 按日期组织笔记

### 日历视图
- 查看当月日历
- 日期旁的圆点表示有任务或笔记
- 点击日期查看当日内容

### WeChat 通知
1. 访问 https://sct.ftqq.com 获取 SendKey
2. 在应用中输入 SendKey 并保存
3. 创建任务时开启提醒功能
4. 任务时间到达时会收到 WeChat 消息

## 移动端使用

### iOS
1. 使用 Safari 打开应用 URL
2. 点击分享按钮
3. 选择 "添加到主屏幕"
4. 点击 "添加"

### Android
1. 使用 Chrome 打开应用 URL
2. 点击菜单按钮
3. 选择 "添加到主屏幕"
4. 点击 "添加"

## 故障排除

### 构建错误
- 检查 package.json 依赖是否正确
- 确认 TypeScript 类型错误
- 查看控制台具体错误信息

### WeChat 通知不工作
- 确认 SendKey 输入正确
- 检查任务时间格式（HH:MM）
- 确认 Server酱 服务状态

### PWA 功能异常
- 确认 manifest.json 配置正确
- 检查 HTTPS 是否启用（生产环境）
- 清除浏览器缓存后重试

## 文件说明

- `src/App.tsx` - 主应用组件
- `src/types.ts` - 类型定义
- `src/components/` - UI 组件
- `src/hooks/` - 自定义 Hook
- `public/manifest.json` - PWA 配置
- `vercel.json` - Vercel 部署配置